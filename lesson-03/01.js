// Напишите функцию calculateAverage, которая принимает 2 числа и возвращает их среднее арифметическое

function calculateAverage(dice1, dice2) {
       return (dice1 + dice2) / 2;
}
console.log(calculateAverage())



















/* let gold = 100 // 70

  function createBuilding (name, cost) {
    if (gold >= cost) {
        gold = gold - cost
        console.log(`${name} completed!`)
    } else {
        console.log(`${name} - not enough resources!`)
    }
    console.log("Rest of gold:", gold)
}

let buildingName = 'house'
let buildingCost = 30
createBuilding(buildingName, buildingCost)

createBuilding('garage', 20)
createBuilding('sauna', 40)

function getSalary (currentGold, increment){
    return currentGold + increment
}

gold = getSalary(gold, 34)
console.log("balance: ", gold)

gold = getSalary(gold, 44)
console.log("balance: ", gold)*/
